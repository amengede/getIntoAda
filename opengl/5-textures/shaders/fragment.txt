#version 460 core

in vec3 fragmentColor;
in vec2 frag_tex_coords;

uniform sampler2D material;

layout(location = 0) out vec4 outColor;

void main()
{
    outColor = vec4(fragmentColor, 1.0) * texture(material, frag_tex_coords);
}
